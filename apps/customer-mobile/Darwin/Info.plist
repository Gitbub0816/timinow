<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
      The identity keys. With GENERATE_INFOPLIST_FILE set to NO, nothing
      injects these — the file is used exactly as written. Without
      CFBundleIdentifier the build succeeds, produces a .app, and then
      devicectl refuses it: "The item at TimiNow.app is not a valid bundle …
      Failed to get the identifier for the app to be installed."
    -->
    <key>CFBundleIdentifier</key><string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleExecutable</key><string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleName</key><string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key><string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
    <key>CFBundleInfoDictionaryVersion</key><string>6.0</string>
    <key>CFBundleDevelopmentRegion</key><string>$(DEVELOPMENT_LANGUAGE)</string>
    <key>CFBundleDisplayName</key><string>Tími NOW</string>
    <!-- See TimiNowApp.swift: the palette has no dark variant. -->
    <key>UIUserInterfaceStyle</key><string>Light</string>
    <!--
      Set from MARKETING_VERSION / CURRENT_PROJECT_VERSION in project.yml.
      They are spelled out here because GENERATE_INFOPLIST_FILE is NO for this
      target, and the build system only injects the version keys when it is
      generating the file itself. Without them an install on a real device and
      every App Store Connect upload fail, and neither error names the plist.
    -->
    <key>CFBundleShortVersionString</key><string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key><string>$(CURRENT_PROJECT_VERSION)</string>
    <!--
      Spelled out for the same reason as the version keys above: actool
      normally injects this when it compiles the app icon, but App Store
      validation rejects the upload if anything about that merge goes
      missing, and a device install never checks.
    -->
    <key>CFBundleIconName</key><string>AppIcon</string>
    <key>ITSAppUsesNonExemptEncryption</key><false/>
    <!--
      Both demanded by turn-by-turn, and one of them under threat of death.

      "audio": MapboxNavigationCore's RouteVoiceController traps at init -
      "Fatal error: This application's Info.plist file must include audio in
      UIBackgroundModes" (RouteVoiceController.swift:114) - so without this
      entry, pressing Navigate with voice enabled closes the app. It is not a
      formality either: it is what lets spoken instructions keep speaking when
      the phone locks in a cradle, which is the normal state of a phone that
      is navigating.

      "location": keeps route progress alive when the app is backgrounded
      mid-drive - a customer who answers a call on the way to a clinic should
      not lose their guidance.
    -->
    <key>UIBackgroundModes</key>
    <array>
        <string>audio</string>
        <string>location</string>
    </array>
    <key>NSLocationWhenInUseUsageDescription</key><string>Tími uses your location to find participating veterinary clinics near you and estimate travel distance.</string>
    <key>NSUserNotificationsUsageDescription</key><string>Tími can notify you when a clinic sends an availability offer or updates your arrival.</string>
    <key>UILaunchScreen</key><dict><key>UIColorName</key><string>LaunchBackground</string></dict>
    <key>UISupportedInterfaceOrientations</key><array><string>UIInterfaceOrientationPortrait</string></array>
    <!--
      CarPlay scene registration. Requires the
      com.apple.developer.carplay-driving-navigation entitlement
      (Darwin/TimiNow.entitlements) — Apple grants this only after a
      separate CarPlay entitlement application, not automatically with a
      developer account. Until that approval lands, iOS never instantiates
      a CPTemplateApplicationScene at all, so CarPlaySceneDelegate
      (Sources/TimiNowCarPlay/CarPlaySceneDelegate.swift) is inert on any
      build signed without the entitlement. See docs/NAVIGATION.md.
    -->
    <key>UIApplicationSceneManifest</key>
    <dict>
        <key>UIApplicationSupportsMultipleScenes</key><true/>
        <key>UISceneConfigurations</key>
        <dict>
            <key>UIWindowSceneSessionRoleApplication</key>
            <array>
                <dict>
                    <key>UISceneConfigurationName</key><string>Default Configuration</string>
                </dict>
            </array>
            <key>CPTemplateApplicationSceneSessionRoleApplication</key>
            <array>
                <dict>
                    <key>UISceneConfigurationName</key><string>CarPlay Configuration</string>
                    <key>UISceneDelegateClassName</key><string>CarPlaySceneDelegate</string>
                </dict>
            </array>
        </dict>
    </dict>
</dict>
</plist>
